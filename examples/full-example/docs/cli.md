<!-- Generated by full-example docs cli --save; do not edit. -->

# full-example — CLI API reference

Argsbarg full example reference app

Machine-readable export: `full-example docs cli-schema`

## `full-example`

Argsbarg full example reference app

#### Subcommands

- `echo` — Echo a message (MCP-friendly leaf).
- `render-json` — Echo a JSON message (schema-first JSON leaf demo).
- `status` — Show app version.
- `workspaces` — Workspace collection and CRUD.

### `full-example echo`

Echo a message (MCP-friendly leaf).

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--message` | string | required | — | Text to print. |

#### Subcommands

- `version` — Print the program version.
- `configure` — Set up agent skills and MCP config for this app (binary via Homebrew).
- `docs` — Print bundled CLI documentation.
- `mcp` — MCP server and bundle tools.
- `http` — HTTP API server for tools.

#### `full-example echo version`

Print the program version.

#### `full-example echo configure`

Set up agent skills and MCP config for this app (binary via Homebrew).

> Set up agent artifacts after the binary is installed via Homebrew (see README for tap install).
> 
> Homebrew post_install runs:
>   full-example configure --sync --yes
> 
> Interactive setup (per target):
>   full-example configure
> 
> Upgrade:
>   brew upgrade full-example
> 
> Shell completions are installed by Homebrew during brew install.
> See: https://docs.brew.sh/Shell-Completion
> 
> See what is installed:
>   full-example configure --status
> 
> Uninstall:
>   brew uninstall <tap>/full-example
> 
> The formula uninstall hook runs `configure --remove-all --yes` (skills, MCP, and app config).
> 
> Use --dry to preview changes without writing files.
> Use --json for machine-readable output.

**Default subcommand:** `run` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--sync` | flag | optional | — | Refresh installed skills and MCP. Used by Homebrew post_install. |
| `--remove-all` | flag | optional | — | Remove all detected agent artifacts (skills and MCP). |
| `--status` | flag | optional | — | Print what is currently installed (read-only). |
| `--yes` (`-y`) | flag | optional | — | Skip confirmation (required for --sync, --remove-all, --remove-config). |
| `--dry` | flag | optional | — | Show what would change without writing files. |
| `--json` | flag | optional | — | Print changed paths or status JSON on stdout. |

#### `full-example echo docs`

Print bundled CLI documentation.

> Topics print to stdout. Add --save to write files under ./docs/.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### Subcommands

- `readme` — Print README (user guide).
- `mcp` — Print MCP server setup and tool guidance.
- `http` — Print HTTP API setup and tool guidance.
- `openapi` — Print the HTTP OpenAPI 3.1 document as JSON.
- `cli-schema` — Print the full CLI command tree as JSON.
- `cli` — Print the full command reference as markdown.
- `skill` — Print a reference agent SKILL; run `configure` to install an optimized copy.

##### `full-example echo docs readme`

Print README (user guide).

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example echo docs mcp`

Print MCP server setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example echo docs http`

Print HTTP API setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example echo docs openapi`

Print the HTTP OpenAPI 3.1 document as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example echo docs cli-schema`

Print the full CLI command tree as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example echo docs cli`

Print the full command reference as markdown.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example echo docs skill`

Print a reference agent SKILL; run `configure` to install an optimized copy.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### `full-example echo mcp`

MCP server and bundle tools.

> Stdio MCP server. Add to Cursor, Claude Code, or Claude Desktop:
> 
>   command: full-example
>   args: mcp
> 
> Or:
> 
>   full-example configure
> 
> Full setup guide: full-example docs mcp

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### Subcommands

- `bundle` — Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

##### `full-example echo mcp bundle`

Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

#### `full-example echo http`

HTTP API server for tools.

> HTTP tool server on http://127.0.0.1:3000.
> 
> Endpoints: GET /health/liveness, GET /health/readiness, GET /openapi.json, GET /swagger, /*
> 
> Configure app settings:
> 
>   full-example configure
> 
> Full setup guide: full-example docs http

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--host` | string | optional | — | Listen host. |
| `--port` | number | optional | — | Listen port. |
| `--trust-proxy` | flag | optional | — | Honor X-Forwarded-For for client IP. |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--no-access-log` | flag | optional | — | Disable HTTP access logs. |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

### `full-example render-json`

Echo a JSON message (schema-first JSON leaf demo).

#### Subcommands

- `version` — Print the program version.
- `configure` — Set up agent skills and MCP config for this app (binary via Homebrew).
- `docs` — Print bundled CLI documentation.
- `mcp` — MCP server and bundle tools.
- `http` — HTTP API server for tools.

#### `full-example render-json version`

Print the program version.

#### `full-example render-json configure`

Set up agent skills and MCP config for this app (binary via Homebrew).

> Set up agent artifacts after the binary is installed via Homebrew (see README for tap install).
> 
> Homebrew post_install runs:
>   full-example configure --sync --yes
> 
> Interactive setup (per target):
>   full-example configure
> 
> Upgrade:
>   brew upgrade full-example
> 
> Shell completions are installed by Homebrew during brew install.
> See: https://docs.brew.sh/Shell-Completion
> 
> See what is installed:
>   full-example configure --status
> 
> Uninstall:
>   brew uninstall <tap>/full-example
> 
> The formula uninstall hook runs `configure --remove-all --yes` (skills, MCP, and app config).
> 
> Use --dry to preview changes without writing files.
> Use --json for machine-readable output.

**Default subcommand:** `run` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--sync` | flag | optional | — | Refresh installed skills and MCP. Used by Homebrew post_install. |
| `--remove-all` | flag | optional | — | Remove all detected agent artifacts (skills and MCP). |
| `--status` | flag | optional | — | Print what is currently installed (read-only). |
| `--yes` (`-y`) | flag | optional | — | Skip confirmation (required for --sync, --remove-all, --remove-config). |
| `--dry` | flag | optional | — | Show what would change without writing files. |
| `--json` | flag | optional | — | Print changed paths or status JSON on stdout. |

#### `full-example render-json docs`

Print bundled CLI documentation.

> Topics print to stdout. Add --save to write files under ./docs/.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### Subcommands

- `readme` — Print README (user guide).
- `mcp` — Print MCP server setup and tool guidance.
- `http` — Print HTTP API setup and tool guidance.
- `openapi` — Print the HTTP OpenAPI 3.1 document as JSON.
- `cli-schema` — Print the full CLI command tree as JSON.
- `cli` — Print the full command reference as markdown.
- `skill` — Print a reference agent SKILL; run `configure` to install an optimized copy.

##### `full-example render-json docs readme`

Print README (user guide).

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example render-json docs mcp`

Print MCP server setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example render-json docs http`

Print HTTP API setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example render-json docs openapi`

Print the HTTP OpenAPI 3.1 document as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example render-json docs cli-schema`

Print the full CLI command tree as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example render-json docs cli`

Print the full command reference as markdown.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example render-json docs skill`

Print a reference agent SKILL; run `configure` to install an optimized copy.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### `full-example render-json mcp`

MCP server and bundle tools.

> Stdio MCP server. Add to Cursor, Claude Code, or Claude Desktop:
> 
>   command: full-example
>   args: mcp
> 
> Or:
> 
>   full-example configure
> 
> Full setup guide: full-example docs mcp

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### Subcommands

- `bundle` — Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

##### `full-example render-json mcp bundle`

Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

#### `full-example render-json http`

HTTP API server for tools.

> HTTP tool server on http://127.0.0.1:3000.
> 
> Endpoints: GET /health/liveness, GET /health/readiness, GET /openapi.json, GET /swagger, /*
> 
> Configure app settings:
> 
>   full-example configure
> 
> Full setup guide: full-example docs http

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--host` | string | optional | — | Listen host. |
| `--port` | number | optional | — | Listen port. |
| `--trust-proxy` | flag | optional | — | Honor X-Forwarded-For for client IP. |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--no-access-log` | flag | optional | — | Disable HTTP access logs. |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

### `full-example status`

Show app version.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--json` | flag | optional | — | Emit JSON. |

#### Output

JSON Schema for output when/if handler emits JSON

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "version": {
      "type": "string",
      "description": "App version from program root."
    }
  },
  "required": [
    "version"
  ],
  "additionalProperties": false,
  "definitions": {}
}
```

#### Subcommands

- `version` — Print the program version.
- `configure` — Set up agent skills and MCP config for this app (binary via Homebrew).
- `docs` — Print bundled CLI documentation.
- `mcp` — MCP server and bundle tools.
- `http` — HTTP API server for tools.

#### `full-example status version`

Print the program version.

#### `full-example status configure`

Set up agent skills and MCP config for this app (binary via Homebrew).

> Set up agent artifacts after the binary is installed via Homebrew (see README for tap install).
> 
> Homebrew post_install runs:
>   full-example configure --sync --yes
> 
> Interactive setup (per target):
>   full-example configure
> 
> Upgrade:
>   brew upgrade full-example
> 
> Shell completions are installed by Homebrew during brew install.
> See: https://docs.brew.sh/Shell-Completion
> 
> See what is installed:
>   full-example configure --status
> 
> Uninstall:
>   brew uninstall <tap>/full-example
> 
> The formula uninstall hook runs `configure --remove-all --yes` (skills, MCP, and app config).
> 
> Use --dry to preview changes without writing files.
> Use --json for machine-readable output.

**Default subcommand:** `run` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--sync` | flag | optional | — | Refresh installed skills and MCP. Used by Homebrew post_install. |
| `--remove-all` | flag | optional | — | Remove all detected agent artifacts (skills and MCP). |
| `--status` | flag | optional | — | Print what is currently installed (read-only). |
| `--yes` (`-y`) | flag | optional | — | Skip confirmation (required for --sync, --remove-all, --remove-config). |
| `--dry` | flag | optional | — | Show what would change without writing files. |
| `--json` | flag | optional | — | Print changed paths or status JSON on stdout. |

#### `full-example status docs`

Print bundled CLI documentation.

> Topics print to stdout. Add --save to write files under ./docs/.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### Subcommands

- `readme` — Print README (user guide).
- `mcp` — Print MCP server setup and tool guidance.
- `http` — Print HTTP API setup and tool guidance.
- `openapi` — Print the HTTP OpenAPI 3.1 document as JSON.
- `cli-schema` — Print the full CLI command tree as JSON.
- `cli` — Print the full command reference as markdown.
- `skill` — Print a reference agent SKILL; run `configure` to install an optimized copy.

##### `full-example status docs readme`

Print README (user guide).

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example status docs mcp`

Print MCP server setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example status docs http`

Print HTTP API setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example status docs openapi`

Print the HTTP OpenAPI 3.1 document as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example status docs cli-schema`

Print the full CLI command tree as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example status docs cli`

Print the full command reference as markdown.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example status docs skill`

Print a reference agent SKILL; run `configure` to install an optimized copy.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### `full-example status mcp`

MCP server and bundle tools.

> Stdio MCP server. Add to Cursor, Claude Code, or Claude Desktop:
> 
>   command: full-example
>   args: mcp
> 
> Or:
> 
>   full-example configure
> 
> Full setup guide: full-example docs mcp

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### Subcommands

- `bundle` — Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

##### `full-example status mcp bundle`

Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

#### `full-example status http`

HTTP API server for tools.

> HTTP tool server on http://127.0.0.1:3000.
> 
> Endpoints: GET /health/liveness, GET /health/readiness, GET /openapi.json, GET /swagger, /*
> 
> Configure app settings:
> 
>   full-example configure
> 
> Full setup guide: full-example docs http

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--host` | string | optional | — | Listen host. |
| `--port` | number | optional | — | Listen port. |
| `--trust-proxy` | flag | optional | — | Honor X-Forwarded-For for client IP. |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--no-access-log` | flag | optional | — | Disable HTTP access logs. |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

### `full-example workspaces`

Workspace collection and CRUD.

#### Subcommands

- `get` — List workspaces.
- `post` — Create a workspace.
- `:id` — One workspace by id.

#### `full-example workspaces get`

List workspaces.

#### Subcommands

- `version` — Print the program version.
- `configure` — Set up agent skills and MCP config for this app (binary via Homebrew).
- `docs` — Print bundled CLI documentation.
- `mcp` — MCP server and bundle tools.
- `http` — HTTP API server for tools.

##### `full-example workspaces get version`

Print the program version.

##### `full-example workspaces get configure`

Set up agent skills and MCP config for this app (binary via Homebrew).

> Set up agent artifacts after the binary is installed via Homebrew (see README for tap install).
> 
> Homebrew post_install runs:
>   full-example configure --sync --yes
> 
> Interactive setup (per target):
>   full-example configure
> 
> Upgrade:
>   brew upgrade full-example
> 
> Shell completions are installed by Homebrew during brew install.
> See: https://docs.brew.sh/Shell-Completion
> 
> See what is installed:
>   full-example configure --status
> 
> Uninstall:
>   brew uninstall <tap>/full-example
> 
> The formula uninstall hook runs `configure --remove-all --yes` (skills, MCP, and app config).
> 
> Use --dry to preview changes without writing files.
> Use --json for machine-readable output.

**Default subcommand:** `run` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--sync` | flag | optional | — | Refresh installed skills and MCP. Used by Homebrew post_install. |
| `--remove-all` | flag | optional | — | Remove all detected agent artifacts (skills and MCP). |
| `--status` | flag | optional | — | Print what is currently installed (read-only). |
| `--yes` (`-y`) | flag | optional | — | Skip confirmation (required for --sync, --remove-all, --remove-config). |
| `--dry` | flag | optional | — | Show what would change without writing files. |
| `--json` | flag | optional | — | Print changed paths or status JSON on stdout. |

##### `full-example workspaces get docs`

Print bundled CLI documentation.

> Topics print to stdout. Add --save to write files under ./docs/.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### Subcommands

- `readme` — Print README (user guide).
- `mcp` — Print MCP server setup and tool guidance.
- `http` — Print HTTP API setup and tool guidance.
- `openapi` — Print the HTTP OpenAPI 3.1 document as JSON.
- `cli-schema` — Print the full CLI command tree as JSON.
- `cli` — Print the full command reference as markdown.
- `skill` — Print a reference agent SKILL; run `configure` to install an optimized copy.

###### `full-example workspaces get docs readme`

Print README (user guide).

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces get docs mcp`

Print MCP server setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces get docs http`

Print HTTP API setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces get docs openapi`

Print the HTTP OpenAPI 3.1 document as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces get docs cli-schema`

Print the full CLI command tree as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces get docs cli`

Print the full command reference as markdown.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces get docs skill`

Print a reference agent SKILL; run `configure` to install an optimized copy.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example workspaces get mcp`

MCP server and bundle tools.

> Stdio MCP server. Add to Cursor, Claude Code, or Claude Desktop:
> 
>   command: full-example
>   args: mcp
> 
> Or:
> 
>   full-example configure
> 
> Full setup guide: full-example docs mcp

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### Subcommands

- `bundle` — Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

###### `full-example workspaces get mcp bundle`

Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

##### `full-example workspaces get http`

HTTP API server for tools.

> HTTP tool server on http://127.0.0.1:3000.
> 
> Endpoints: GET /health/liveness, GET /health/readiness, GET /openapi.json, GET /swagger, /*
> 
> Configure app settings:
> 
>   full-example configure
> 
> Full setup guide: full-example docs http

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--host` | string | optional | — | Listen host. |
| `--port` | number | optional | — | Listen port. |
| `--trust-proxy` | flag | optional | — | Honor X-Forwarded-For for client IP. |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--no-access-log` | flag | optional | — | Disable HTTP access logs. |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### `full-example workspaces post`

Create a workspace.

#### Subcommands

- `version` — Print the program version.
- `configure` — Set up agent skills and MCP config for this app (binary via Homebrew).
- `docs` — Print bundled CLI documentation.
- `mcp` — MCP server and bundle tools.
- `http` — HTTP API server for tools.

##### `full-example workspaces post version`

Print the program version.

##### `full-example workspaces post configure`

Set up agent skills and MCP config for this app (binary via Homebrew).

> Set up agent artifacts after the binary is installed via Homebrew (see README for tap install).
> 
> Homebrew post_install runs:
>   full-example configure --sync --yes
> 
> Interactive setup (per target):
>   full-example configure
> 
> Upgrade:
>   brew upgrade full-example
> 
> Shell completions are installed by Homebrew during brew install.
> See: https://docs.brew.sh/Shell-Completion
> 
> See what is installed:
>   full-example configure --status
> 
> Uninstall:
>   brew uninstall <tap>/full-example
> 
> The formula uninstall hook runs `configure --remove-all --yes` (skills, MCP, and app config).
> 
> Use --dry to preview changes without writing files.
> Use --json for machine-readable output.

**Default subcommand:** `run` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--sync` | flag | optional | — | Refresh installed skills and MCP. Used by Homebrew post_install. |
| `--remove-all` | flag | optional | — | Remove all detected agent artifacts (skills and MCP). |
| `--status` | flag | optional | — | Print what is currently installed (read-only). |
| `--yes` (`-y`) | flag | optional | — | Skip confirmation (required for --sync, --remove-all, --remove-config). |
| `--dry` | flag | optional | — | Show what would change without writing files. |
| `--json` | flag | optional | — | Print changed paths or status JSON on stdout. |

##### `full-example workspaces post docs`

Print bundled CLI documentation.

> Topics print to stdout. Add --save to write files under ./docs/.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### Subcommands

- `readme` — Print README (user guide).
- `mcp` — Print MCP server setup and tool guidance.
- `http` — Print HTTP API setup and tool guidance.
- `openapi` — Print the HTTP OpenAPI 3.1 document as JSON.
- `cli-schema` — Print the full CLI command tree as JSON.
- `cli` — Print the full command reference as markdown.
- `skill` — Print a reference agent SKILL; run `configure` to install an optimized copy.

###### `full-example workspaces post docs readme`

Print README (user guide).

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces post docs mcp`

Print MCP server setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces post docs http`

Print HTTP API setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces post docs openapi`

Print the HTTP OpenAPI 3.1 document as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces post docs cli-schema`

Print the full CLI command tree as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces post docs cli`

Print the full command reference as markdown.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces post docs skill`

Print a reference agent SKILL; run `configure` to install an optimized copy.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

##### `full-example workspaces post mcp`

MCP server and bundle tools.

> Stdio MCP server. Add to Cursor, Claude Code, or Claude Desktop:
> 
>   command: full-example
>   args: mcp
> 
> Or:
> 
>   full-example configure
> 
> Full setup guide: full-example docs mcp

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### Subcommands

- `bundle` — Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

###### `full-example workspaces post mcp bundle`

Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

##### `full-example workspaces post http`

HTTP API server for tools.

> HTTP tool server on http://127.0.0.1:3000.
> 
> Endpoints: GET /health/liveness, GET /health/readiness, GET /openapi.json, GET /swagger, /*
> 
> Configure app settings:
> 
>   full-example configure
> 
> Full setup guide: full-example docs http

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--host` | string | optional | — | Listen host. |
| `--port` | number | optional | — | Listen port. |
| `--trust-proxy` | flag | optional | — | Honor X-Forwarded-For for client IP. |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--no-access-log` | flag | optional | — | Disable HTTP access logs. |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### `full-example workspaces :id`

One workspace by id.

#### Subcommands

- `get` — Get one workspace.
- `put` — Replace a workspace.
- `patch` — Patch a workspace name.
- `delete` — Delete a workspace.

##### `full-example workspaces :id get`

Get one workspace.

#### Subcommands

- `version` — Print the program version.
- `configure` — Set up agent skills and MCP config for this app (binary via Homebrew).
- `docs` — Print bundled CLI documentation.
- `mcp` — MCP server and bundle tools.
- `http` — HTTP API server for tools.

###### `full-example workspaces :id get version`

Print the program version.

###### `full-example workspaces :id get configure`

Set up agent skills and MCP config for this app (binary via Homebrew).

> Set up agent artifacts after the binary is installed via Homebrew (see README for tap install).
> 
> Homebrew post_install runs:
>   full-example configure --sync --yes
> 
> Interactive setup (per target):
>   full-example configure
> 
> Upgrade:
>   brew upgrade full-example
> 
> Shell completions are installed by Homebrew during brew install.
> See: https://docs.brew.sh/Shell-Completion
> 
> See what is installed:
>   full-example configure --status
> 
> Uninstall:
>   brew uninstall <tap>/full-example
> 
> The formula uninstall hook runs `configure --remove-all --yes` (skills, MCP, and app config).
> 
> Use --dry to preview changes without writing files.
> Use --json for machine-readable output.

**Default subcommand:** `run` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--sync` | flag | optional | — | Refresh installed skills and MCP. Used by Homebrew post_install. |
| `--remove-all` | flag | optional | — | Remove all detected agent artifacts (skills and MCP). |
| `--status` | flag | optional | — | Print what is currently installed (read-only). |
| `--yes` (`-y`) | flag | optional | — | Skip confirmation (required for --sync, --remove-all, --remove-config). |
| `--dry` | flag | optional | — | Show what would change without writing files. |
| `--json` | flag | optional | — | Print changed paths or status JSON on stdout. |

###### `full-example workspaces :id get docs`

Print bundled CLI documentation.

> Topics print to stdout. Add --save to write files under ./docs/.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### Subcommands

- `readme` — Print README (user guide).
- `mcp` — Print MCP server setup and tool guidance.
- `http` — Print HTTP API setup and tool guidance.
- `openapi` — Print the HTTP OpenAPI 3.1 document as JSON.
- `cli-schema` — Print the full CLI command tree as JSON.
- `cli` — Print the full command reference as markdown.
- `skill` — Print a reference agent SKILL; run `configure` to install an optimized copy.

###### `full-example workspaces :id get docs readme`

Print README (user guide).

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id get docs mcp`

Print MCP server setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id get docs http`

Print HTTP API setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id get docs openapi`

Print the HTTP OpenAPI 3.1 document as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id get docs cli-schema`

Print the full CLI command tree as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id get docs cli`

Print the full command reference as markdown.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id get docs skill`

Print a reference agent SKILL; run `configure` to install an optimized copy.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id get mcp`

MCP server and bundle tools.

> Stdio MCP server. Add to Cursor, Claude Code, or Claude Desktop:
> 
>   command: full-example
>   args: mcp
> 
> Or:
> 
>   full-example configure
> 
> Full setup guide: full-example docs mcp

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### Subcommands

- `bundle` — Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

###### `full-example workspaces :id get mcp bundle`

Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

###### `full-example workspaces :id get http`

HTTP API server for tools.

> HTTP tool server on http://127.0.0.1:3000.
> 
> Endpoints: GET /health/liveness, GET /health/readiness, GET /openapi.json, GET /swagger, /*
> 
> Configure app settings:
> 
>   full-example configure
> 
> Full setup guide: full-example docs http

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--host` | string | optional | — | Listen host. |
| `--port` | number | optional | — | Listen port. |
| `--trust-proxy` | flag | optional | — | Honor X-Forwarded-For for client IP. |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--no-access-log` | flag | optional | — | Disable HTTP access logs. |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

##### `full-example workspaces :id put`

Replace a workspace.

#### Subcommands

- `version` — Print the program version.
- `configure` — Set up agent skills and MCP config for this app (binary via Homebrew).
- `docs` — Print bundled CLI documentation.
- `mcp` — MCP server and bundle tools.
- `http` — HTTP API server for tools.

###### `full-example workspaces :id put version`

Print the program version.

###### `full-example workspaces :id put configure`

Set up agent skills and MCP config for this app (binary via Homebrew).

> Set up agent artifacts after the binary is installed via Homebrew (see README for tap install).
> 
> Homebrew post_install runs:
>   full-example configure --sync --yes
> 
> Interactive setup (per target):
>   full-example configure
> 
> Upgrade:
>   brew upgrade full-example
> 
> Shell completions are installed by Homebrew during brew install.
> See: https://docs.brew.sh/Shell-Completion
> 
> See what is installed:
>   full-example configure --status
> 
> Uninstall:
>   brew uninstall <tap>/full-example
> 
> The formula uninstall hook runs `configure --remove-all --yes` (skills, MCP, and app config).
> 
> Use --dry to preview changes without writing files.
> Use --json for machine-readable output.

**Default subcommand:** `run` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--sync` | flag | optional | — | Refresh installed skills and MCP. Used by Homebrew post_install. |
| `--remove-all` | flag | optional | — | Remove all detected agent artifacts (skills and MCP). |
| `--status` | flag | optional | — | Print what is currently installed (read-only). |
| `--yes` (`-y`) | flag | optional | — | Skip confirmation (required for --sync, --remove-all, --remove-config). |
| `--dry` | flag | optional | — | Show what would change without writing files. |
| `--json` | flag | optional | — | Print changed paths or status JSON on stdout. |

###### `full-example workspaces :id put docs`

Print bundled CLI documentation.

> Topics print to stdout. Add --save to write files under ./docs/.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### Subcommands

- `readme` — Print README (user guide).
- `mcp` — Print MCP server setup and tool guidance.
- `http` — Print HTTP API setup and tool guidance.
- `openapi` — Print the HTTP OpenAPI 3.1 document as JSON.
- `cli-schema` — Print the full CLI command tree as JSON.
- `cli` — Print the full command reference as markdown.
- `skill` — Print a reference agent SKILL; run `configure` to install an optimized copy.

###### `full-example workspaces :id put docs readme`

Print README (user guide).

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id put docs mcp`

Print MCP server setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id put docs http`

Print HTTP API setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id put docs openapi`

Print the HTTP OpenAPI 3.1 document as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id put docs cli-schema`

Print the full CLI command tree as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id put docs cli`

Print the full command reference as markdown.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id put docs skill`

Print a reference agent SKILL; run `configure` to install an optimized copy.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id put mcp`

MCP server and bundle tools.

> Stdio MCP server. Add to Cursor, Claude Code, or Claude Desktop:
> 
>   command: full-example
>   args: mcp
> 
> Or:
> 
>   full-example configure
> 
> Full setup guide: full-example docs mcp

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### Subcommands

- `bundle` — Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

###### `full-example workspaces :id put mcp bundle`

Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

###### `full-example workspaces :id put http`

HTTP API server for tools.

> HTTP tool server on http://127.0.0.1:3000.
> 
> Endpoints: GET /health/liveness, GET /health/readiness, GET /openapi.json, GET /swagger, /*
> 
> Configure app settings:
> 
>   full-example configure
> 
> Full setup guide: full-example docs http

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--host` | string | optional | — | Listen host. |
| `--port` | number | optional | — | Listen port. |
| `--trust-proxy` | flag | optional | — | Honor X-Forwarded-For for client IP. |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--no-access-log` | flag | optional | — | Disable HTTP access logs. |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

##### `full-example workspaces :id patch`

Patch a workspace name.

#### Subcommands

- `version` — Print the program version.
- `configure` — Set up agent skills and MCP config for this app (binary via Homebrew).
- `docs` — Print bundled CLI documentation.
- `mcp` — MCP server and bundle tools.
- `http` — HTTP API server for tools.

###### `full-example workspaces :id patch version`

Print the program version.

###### `full-example workspaces :id patch configure`

Set up agent skills and MCP config for this app (binary via Homebrew).

> Set up agent artifacts after the binary is installed via Homebrew (see README for tap install).
> 
> Homebrew post_install runs:
>   full-example configure --sync --yes
> 
> Interactive setup (per target):
>   full-example configure
> 
> Upgrade:
>   brew upgrade full-example
> 
> Shell completions are installed by Homebrew during brew install.
> See: https://docs.brew.sh/Shell-Completion
> 
> See what is installed:
>   full-example configure --status
> 
> Uninstall:
>   brew uninstall <tap>/full-example
> 
> The formula uninstall hook runs `configure --remove-all --yes` (skills, MCP, and app config).
> 
> Use --dry to preview changes without writing files.
> Use --json for machine-readable output.

**Default subcommand:** `run` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--sync` | flag | optional | — | Refresh installed skills and MCP. Used by Homebrew post_install. |
| `--remove-all` | flag | optional | — | Remove all detected agent artifacts (skills and MCP). |
| `--status` | flag | optional | — | Print what is currently installed (read-only). |
| `--yes` (`-y`) | flag | optional | — | Skip confirmation (required for --sync, --remove-all, --remove-config). |
| `--dry` | flag | optional | — | Show what would change without writing files. |
| `--json` | flag | optional | — | Print changed paths or status JSON on stdout. |

###### `full-example workspaces :id patch docs`

Print bundled CLI documentation.

> Topics print to stdout. Add --save to write files under ./docs/.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### Subcommands

- `readme` — Print README (user guide).
- `mcp` — Print MCP server setup and tool guidance.
- `http` — Print HTTP API setup and tool guidance.
- `openapi` — Print the HTTP OpenAPI 3.1 document as JSON.
- `cli-schema` — Print the full CLI command tree as JSON.
- `cli` — Print the full command reference as markdown.
- `skill` — Print a reference agent SKILL; run `configure` to install an optimized copy.

###### `full-example workspaces :id patch docs readme`

Print README (user guide).

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id patch docs mcp`

Print MCP server setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id patch docs http`

Print HTTP API setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id patch docs openapi`

Print the HTTP OpenAPI 3.1 document as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id patch docs cli-schema`

Print the full CLI command tree as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id patch docs cli`

Print the full command reference as markdown.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id patch docs skill`

Print a reference agent SKILL; run `configure` to install an optimized copy.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id patch mcp`

MCP server and bundle tools.

> Stdio MCP server. Add to Cursor, Claude Code, or Claude Desktop:
> 
>   command: full-example
>   args: mcp
> 
> Or:
> 
>   full-example configure
> 
> Full setup guide: full-example docs mcp

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### Subcommands

- `bundle` — Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

###### `full-example workspaces :id patch mcp bundle`

Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

###### `full-example workspaces :id patch http`

HTTP API server for tools.

> HTTP tool server on http://127.0.0.1:3000.
> 
> Endpoints: GET /health/liveness, GET /health/readiness, GET /openapi.json, GET /swagger, /*
> 
> Configure app settings:
> 
>   full-example configure
> 
> Full setup guide: full-example docs http

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--host` | string | optional | — | Listen host. |
| `--port` | number | optional | — | Listen port. |
| `--trust-proxy` | flag | optional | — | Honor X-Forwarded-For for client IP. |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--no-access-log` | flag | optional | — | Disable HTTP access logs. |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

##### `full-example workspaces :id delete`

Delete a workspace.

#### Subcommands

- `version` — Print the program version.
- `configure` — Set up agent skills and MCP config for this app (binary via Homebrew).
- `docs` — Print bundled CLI documentation.
- `mcp` — MCP server and bundle tools.
- `http` — HTTP API server for tools.

###### `full-example workspaces :id delete version`

Print the program version.

###### `full-example workspaces :id delete configure`

Set up agent skills and MCP config for this app (binary via Homebrew).

> Set up agent artifacts after the binary is installed via Homebrew (see README for tap install).
> 
> Homebrew post_install runs:
>   full-example configure --sync --yes
> 
> Interactive setup (per target):
>   full-example configure
> 
> Upgrade:
>   brew upgrade full-example
> 
> Shell completions are installed by Homebrew during brew install.
> See: https://docs.brew.sh/Shell-Completion
> 
> See what is installed:
>   full-example configure --status
> 
> Uninstall:
>   brew uninstall <tap>/full-example
> 
> The formula uninstall hook runs `configure --remove-all --yes` (skills, MCP, and app config).
> 
> Use --dry to preview changes without writing files.
> Use --json for machine-readable output.

**Default subcommand:** `run` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--sync` | flag | optional | — | Refresh installed skills and MCP. Used by Homebrew post_install. |
| `--remove-all` | flag | optional | — | Remove all detected agent artifacts (skills and MCP). |
| `--status` | flag | optional | — | Print what is currently installed (read-only). |
| `--yes` (`-y`) | flag | optional | — | Skip confirmation (required for --sync, --remove-all, --remove-config). |
| `--dry` | flag | optional | — | Show what would change without writing files. |
| `--json` | flag | optional | — | Print changed paths or status JSON on stdout. |

###### `full-example workspaces :id delete docs`

Print bundled CLI documentation.

> Topics print to stdout. Add --save to write files under ./docs/.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

#### Subcommands

- `readme` — Print README (user guide).
- `mcp` — Print MCP server setup and tool guidance.
- `http` — Print HTTP API setup and tool guidance.
- `openapi` — Print the HTTP OpenAPI 3.1 document as JSON.
- `cli-schema` — Print the full CLI command tree as JSON.
- `cli` — Print the full command reference as markdown.
- `skill` — Print a reference agent SKILL; run `configure` to install an optimized copy.

###### `full-example workspaces :id delete docs readme`

Print README (user guide).

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id delete docs mcp`

Print MCP server setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id delete docs http`

Print HTTP API setup and tool guidance.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id delete docs openapi`

Print the HTTP OpenAPI 3.1 document as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id delete docs cli-schema`

Print the full CLI command tree as JSON.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id delete docs cli`

Print the full command reference as markdown.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id delete docs skill`

Print a reference agent SKILL; run `configure` to install an optimized copy.

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--save` | flag | optional | — | Write documentation to ./docs/. |

###### `full-example workspaces :id delete mcp`

MCP server and bundle tools.

> Stdio MCP server. Add to Cursor, Claude Code, or Claude Desktop:
> 
>   command: full-example
>   args: mcp
> 
> Or:
> 
>   full-example configure
> 
> Full setup guide: full-example docs mcp

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |

#### Subcommands

- `bundle` — Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

###### `full-example workspaces :id delete mcp bundle`

Pack dist MCP artifacts (`.mcpb`, Claude Code plugin zip) from dist/<key>.

###### `full-example workspaces :id delete http`

HTTP API server for tools.

> HTTP tool server on http://127.0.0.1:3000.
> 
> Endpoints: GET /health/liveness, GET /health/readiness, GET /openapi.json, GET /swagger, /*
> 
> Configure app settings:
> 
>   full-example configure
> 
> Full setup guide: full-example docs http

**Default subcommand:** `serve` (`missingOnly`)

#### Options

| Option | Type | Required | Format / default | Description |
| --- | --- | --- | --- | --- |
| `--host` | string | optional | — | Listen host. |
| `--port` | number | optional | — | Listen port. |
| `--trust-proxy` | flag | optional | — | Honor X-Forwarded-For for client IP. |
| `--obscure-errors` | flag | optional | — | Hide unexpected errors from clients. |
| `--log-format` | enum (`json`, `text`) | optional | — | Log format: json (ECS) or text. |
| `--log-file` | string | optional | — | Append logs to this file (relative → app config dir). |
| `--no-access-log` | flag | optional | — | Disable HTTP access logs. |
| `--dev` | flag | optional | — | Print full stacks to stderr on errors. |
